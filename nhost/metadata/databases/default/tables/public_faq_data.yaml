table:
  name: faq_data
  schema: public
select_permissions:
  - role: manager
    permission:
      columns:
        - id
        - question
        - answer
        - is_client
        - is_worker
      filter: {}
  - role: worker
    permission:
      columns:
        - is_client
        - is_worker
        - id
        - answer
        - question
      filter: {}
