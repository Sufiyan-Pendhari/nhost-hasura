table:
  name: vaccine
  schema: public
array_relationships:
  - name: mandatory_vaccine
    using:
      manual_configuration:
        column_mapping:
          id: vacination_id
        insertion_order: null
        remote_table:
          name: mandatory_vaccination
          schema: public
select_permissions:
  - role: worker
    permission:
      columns:
        - id
        - name
      filter: {}
