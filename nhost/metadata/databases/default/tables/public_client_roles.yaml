table:
  name: client_roles
  schema: public
select_permissions:
  - role: manager
    permission:
      columns:
        - description
        - id
        - name
        - parent_id
      filter: {}
