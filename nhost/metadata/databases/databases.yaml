- name: sembadb
  kind: postgres
  configuration:
    connection_info:
      use_prepared_statements: false
      database_url:
        from_env: HASURA_GRAPHQL_DATABASE_URL
      isolation_level: read-committed
  customization:
    naming_convention: graphql-default
  tables: "!include sembadb/tables/tables.yaml"
